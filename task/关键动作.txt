梳理事件批次、公钥、端点策略、幂等账本、历史尝试和两份业务合同之间的字段关系。
使用Node.js crypto按合同生成canonical message并完成Ed25519分离签名核对。
依照intake_contract.json的裁决顺序处理时间窗口、历史幂等、批内重复、路由状态、字段完整性和尝试上限。
结合attempt_history.csv与endpoint_policy.csv计算下一尝试号和指数退避时刻。
生成全量裁决台账与重放队列，使event_id、delivery_id和endpoint_id能够交叉定位。
按端点策略汇总排队数、拒绝数和最早投递时刻，形成维护窗口交接表。
联查三份交付中的事件去向、端点归属、排队数量和最早投递时刻，为维护窗口交接做准备。
