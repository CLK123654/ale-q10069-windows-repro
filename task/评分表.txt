检查项：交付路径与可读性
标准答案：src/rebuild_webhook_queue.mjs、deliverables/intake_decisions.csv、deliverables/replay_queue.jsonl和deliverables/endpoint_handoff.csv均存在，源码可由Node.js24运行，CSV与JSONL可解析。
关键字段：上述四个相对路径。
容差：路径和扩展名逐字匹配rules/output_contract.json，不接受缺少核心文件。
失败边界：任一文件缺失、为空或无法解析。
可观察证据：文件清单、Node.js退出码、CSV表头和逐行JSON解析结果。
客观硬性标准：四个交付物全部可读且入口返回0。
阈值：全部满足。
优先级：Gate项
所属层级：L1任务指令遵循层

检查项：Ed25519签名载荷与验签
标准答案：canonical message依次使用provider、delivery_id、created_at_utc和递归键序JSON body，并以换行连接；signature按base64url解码并由对应公钥通过crypto.verify核对。E003裁决为invalid_signature，E004裁决为unknown_provider，其余进入后续规则的已知provider事件签名有效。
关键字段：rules/intake_contract.json的signature_contract、security/provider_keys.json、incoming/webhook_events.jsonl、deliverables/intake_decisions.csv。
容差：字段顺序和分隔符不设容差，来源为signature_contract；JSON对象键序归一化，数组顺序保留。
失败边界：绕过crypto.verify、使用错误provider公钥、普通字符串比对或canonical message字段顺序错误。
可观察证据：完成版源码中的createPublicKey与verify调用、E003和E004的decision及reason。
客观硬性标准：两条指定裁决正确，所有排队事件均通过真实Ed25519验签。
阈值：全部满足。
优先级：高权重项
所属层级：L2软件或工具操作层

检查项：接收窗口与两层幂等
标准答案：E005为timestamp_out_of_window；E002为duplicate_in_ledger；E011进入队列，后到的E012为duplicate_in_batch。历史幂等键和批内幂等键均由provider与delivery_id组成。
关键字段：rules/intake_contract.json的received_window_seconds与idempotency_window_hours、history/idempotency_ledger.csv、deliverables/intake_decisions.csv。
容差：时间比较精确到整秒；窗口值直接取合同字段。
失败边界：秒与毫秒混用、遗漏provider、把账本窗口外记录当成重复，或在E011之前占用批内键。
可观察证据：四个event_id对应的decision、reason和detail。
客观硬性标准：四条裁决与标准答案一致。
阈值：全部满足。
优先级：高权重项
所属层级：L6领域计算与专业规则层

检查项：路由、字段合同与尝试上限
标准答案：E006为unsupported_event_type，E007为endpoint_inactive，E008为schema_missing_field且缺少renditions，E010为max_attempts_exhausted。
关键字段：routing/endpoint_policy.csv、rules/intake_contract.json的required_body_fields、history/attempt_history.csv、deliverables/intake_decisions.csv。
容差：事件类型、active和字段名按输入字面比较；尝试次数按event_id聚合。
失败边界：把未配置端点与停用端点混为一类、跨事件类型套用字段或将达到上限的事件排队。
可观察证据：四条裁决的decision、reason、endpoint_id和detail。
客观硬性标准：四条裁决逐项一致。
阈值：全部满足。
优先级：高权重项
所属层级：L5多源关联与字段一致性层

检查项：重放队列与退避时刻
标准答案：队列事件集合为E001、E009、E011、E013。E001和E011的attempt_no为1且scheduled_at_utc等于received_at_utc；E009为attempt_no3并安排在2026-07-30T09:20:00Z；E013为attempt_no2并安排在2026-07-30T09:22:00Z。队列按scheduled_at_utc和event_id升序。
关键字段：history/attempt_history.csv、routing/endpoint_policy.csv的base_delay_seconds、rules/intake_contract.json的retry_policy、deliverables/replay_queue.jsonl。
容差：时间精确到1秒，公式与排序来自合同。
失败边界：排入被拒事件、遗漏排队事件、attempt_no偏移、退避幂次错误或排序不符。
可观察证据：replay_queue.jsonl的event_id、attempt_no、scheduled_at_utc和endpoint_id。
客观硬性标准：四条队列记录的集合、字段和顺序全部一致。
阈值：全部满足。
优先级：高权重项
所属层级：L7产物结构或Schema或专业对象层

检查项：全量裁决台账
标准答案：intake_decisions.csv含13条事件且保持输入顺序。queue事件为E001、E009、E011、E013，其余9条为reject；拒绝原因依次覆盖duplicate_in_ledger、invalid_signature、unknown_provider、timestamp_out_of_window、unsupported_event_type、endpoint_inactive、schema_missing_field、max_attempts_exhausted和duplicate_in_batch。
关键字段：incoming/webhook_events.jsonl、deliverables/intake_decisions.csv。
容差：事件顺序按输入逐行位置；reason使用rules/intake_contract.json的reason_codes。
失败边界：事件缺失或重复、一个事件有多个终态、queue与replay_queue.jsonl不一致。
可观察证据：按event_id连接裁决台账和重放队列。
客观硬性标准：13个event_id各出现一次，queue集合和9种拒绝原因均正确。
阈值：全部满足。
优先级：高权重项
所属层级：L9跨产物一致性与可追溯层

检查项：端点维护窗口交接
标准答案：endpoint_handoff.csv按策略顺序列出billing-ledger、media-publisher和payout-ops。queued_count分别为2、2、0，rejected_count分别为4、3、1，earliest_scheduled_at_utc分别为2026-07-30T09:00:20Z、2026-07-30T09:10:07Z和空值，active与max_attempts和策略表一致。
关键字段：routing/endpoint_policy.csv、deliverables/intake_decisions.csv、deliverables/replay_queue.jsonl、deliverables/endpoint_handoff.csv。
容差：计数按event_type和endpoint_id复算，时间精确到1秒。
失败边界：未配置事件进入端点交接、排队与拒绝重复计数、空队列端点填入虚构时间。
可观察证据：三行端点数据与前两份业务文件的聚合结果。
客观硬性标准：三行顺序、计数、状态、上限和最早时间全部一致。
阈值：全部满足。
优先级：普通项
所属层级：L9跨产物一致性与可追溯层
