# 合作方Webhook重放队列

本仓库收录维护窗口重放队列任务的题面、输入材料、标准交付和任务规格。业务程序使用Node.js24核对Ed25519签名，结合幂等账本、端点策略与投递历史生成裁决台账、重放队列和端点交接表。

解压输入数据包后，在系统终端中进入input_data目录并执行npm run rebuild。该批次使用本次开发者平台Webhook接入数据，合作方与端点字段已脱敏，运行期间不访问端点地址或其他外部服务。
