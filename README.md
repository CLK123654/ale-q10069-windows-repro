# 合作方Webhook重放队列

本仓库收录维护窗口重放队列任务的题面、输入材料、标准交付和任务规格。业务程序使用Node.js24核对Ed25519签名，结合幂等账本、端点策略与投递历史生成裁决台账、重放队列和端点交接表。

解压输入数据包后，在Windows的PowerShell中进入input_data目录并执行npm run rebuild。该批次为虚构的开发者平台样例，运行期间不访问端点地址或其他外部服务。
